# -*- coding: utf-8 -*-

from jubeat_online.Scores import Songs

song_obj = Songs()
song_obj.update_song()

# create table song(sid int, title varchar(100), artist varchar(100), bas_level int, adv_level int, ext_level int, primary key(sid))
